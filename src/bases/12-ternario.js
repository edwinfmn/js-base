

const activo = true;

// let mensaje = '';
//
// if (activo) {
//   mensaje ='Activo';
// } else {
//   mensaje = 'Inactivo';
// }
// validacion if resumido
// const mensaje = ( activo ) ? 'Activo' : 'Inactivo';

// validacion if resumido con else null
// const mensaje = ( activo ) ? 'Activo' : null;

// validacion solo positivo
const mensaje = activo && 'Activo'

console.log(mensaje);
